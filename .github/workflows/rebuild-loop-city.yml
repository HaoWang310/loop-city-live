name: Rebuild Loop City live app

on:
  push:
    branches:
      - main
    paths:
      - "app/template_combined_v10.html"
      - "html support files/js/**"
      - "assets/**"
      - "build/build_app.py"
      - "build/build_cell.py"
      - "html support files/cell/wool/wool_en.html"
      - ".github/workflows/rebuild-loop-city.yml"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: rebuild-loop-city
  cancel-in-progress: true

jobs:
  rebuild:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install build dependency
        run: python -m pip install --disable-pip-version-check numpy

      - name: Rebuild embedded Wool app
        run: python build/build_cell.py

      - name: Rebuild combined app
        env:
          LOOP_CITY_NO_ARCHIVE: "1"
        run: python build/build_app.py

      - name: Commit generated live page
        shell: bash
        run: |
          if git diff --quiet -- "html support files/combined_v10.html" "html support files/cell/wool/wool_app.js"; then
            echo "Generated pages are already current."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add "html support files/combined_v10.html" "html support files/cell/wool/wool_app.js"
          git commit -m "Auto-rebuild Loop City live pages"
          git push
