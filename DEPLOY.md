# Loop City — GitHub Pages build

This folder is ready to publish from the repository root with GitHub Pages.

Entry point: `index.html`

The original `Loop City.html` is retained for compatibility. The Pages version uses the same project files and relative paths.
